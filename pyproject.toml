[tool.pytest.ini_options]
testpaths = ["tests"]
# Project-local temp root: hermetic regardless of the machine's shared
# pytest temp state, and covered by .gitignore. (No [project] table on
# purpose: this is a reference repo run with `python -m`, not a package.)
addopts = "-p no:cacheprovider --basetemp=.pytest-tmp"
